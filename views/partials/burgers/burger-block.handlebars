<li>
	{{burger_name}}

	<button type="button" class="btn btn-outline-danger btn-sm" class="eaten-state" id="devour-btn" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#unless devoured}}DEVOUR IT!{{/unless}}
	</button>

</li>